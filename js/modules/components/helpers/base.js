class Helper {
  constructor (element) {
    this.$element = element;
  }

  /**
   * @param {*} selection
   * 
   * Returns the executed test of selection
   */
  test(selection) {}

  /**
   * Executes command
   */
  command() {}

  /**
   * @param {*} selection
   * 
   * Replaces selection into HTML element
   */
  exec(selection) {}
}