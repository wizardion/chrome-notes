class Helper {
  constructor () {}

  /**
   * @param {*} selection
   * 
   * Returns the executed test of selection
   */
  test(selection) {}

  /**
   * Executes command
   */
  command() {}

  /**
   * @param {*} selection
   * 
   * Replaces selection into HTML element
   */
  exec(selection) {}
}